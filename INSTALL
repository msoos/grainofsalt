======================
Compile with cmake
======================
Go into the "build/" directory, and issue:

cmake ../
make

You will then have the "grainofsalt" tool in the "build" directory.



======================
Compile with autotools
======================
Go to the 'build' directory. Issue

../configure
make

You will then have the "grainofsalt" tool in the "build" directory.

